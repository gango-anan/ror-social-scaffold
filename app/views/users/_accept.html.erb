<% @user ||= current_user %>
<%= form_with(model: current_user.indirect_friendships.build,url: edit_bond_path, method: 'patch', local: true) do |f| %>
    <div><%= hidden_field_tag :user_id, @user.id %></div>
    <%= f.submit 'Accept Request'%>
<% end %>