<% @user ||= current_user %>
<%= form_with(model: current_user.direct_friendships.build, local: true) do |f| %>
    <div><%= hidden_field_tag :friend_id, @user.id %></div>
    <%= f.submit 'Invite to Friendship'%>
<% end %>